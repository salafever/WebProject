TODO:

Signup page:
       Fix the submit button
       Don't let the user sign up unless the fields are correct
       Don't let the user click the button until the box is checked
       Don't let them sign in if a user of the same name exists
       Log them in automatically, take them to the logged in page
       Button needs to return you to the main page


Account Settings:
	Have javascript send TRUE for the cookie, we're checking for cookies
	Push the update account button, take everything in the fields and update
	the database with it
	People sign in with Names and Email. Either they change it on the account
	or they aren't allowed to change it at all. 


MainPage:
	Have Javascript check if the cookie is set. We can do this through document
	cookie?

	Redirect to either the mainPage.html if you're logged out,
	Or to the loggedIn.html if you're logged in

LoggedIn:
	If anyone tries to go to this page using the URL, automatically redirect them
	to the main page if they aren't logged in

	Have an AJAX call to the database returning all entries including the city, state,
	name, job title, linkedin

	Place it on the map with Google Maps API, map pointers have links to the person's
	information


InfoPage:

	HTML to get that
	AJAX call to get all the person's information, no way to edit. Just for viewing.
	
